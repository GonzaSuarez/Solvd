SELECT AVG(capacity)
FROM Plane;

SELECT COUNT(idUser)
FROM User;

SELECT SUM(current_price)
FROM Room;

SELECT MAX(max_room)
FROM plan;

SELECT MIN(max_room)
FROM plan;

SELECT AVG(current_price)
FROM Room;

SELECT MIN(monthly_price)
FROM plan;